import 'package:flutter/widgets.dart';

class AppAnalytics extends StatelessWidget {
  const AppAnalytics({
    required this.child,
    super.key,
  });

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
