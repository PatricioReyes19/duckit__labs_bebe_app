# onboarding

Flujo de activación inicial de BebéApp: elección contextual, invitaciones,
creación del perfil del bebé y confirmación. La UI está organizada en
`bloc/`, `pages/` y `views/`; la persistencia depende de
`OnboardingRepository`, reemplazable por un adaptador Firebase.
