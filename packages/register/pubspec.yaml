name: register
description: Presentational register-event package for BebéApp.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  core:
  design_system:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  go_router: ^16.3.0
  image_picker: ^1.2.3

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
