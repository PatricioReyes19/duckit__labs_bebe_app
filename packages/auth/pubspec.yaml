name: auth
description: Dominio y adaptadores de autenticación desacoplados de la UI para BebéApp.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.22.0"

resolution: workspace

dependencies:
  firebase_auth: ^6.5.7
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5
  core:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  shared_preferences_platform_interface: ^2.4.2
