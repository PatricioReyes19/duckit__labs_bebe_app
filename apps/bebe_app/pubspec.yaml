name: bebe_app
description: DuckIT Labs BebéApp mobile application.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

platforms:
  android:
  ios:

dependencies:
  flutter:
    sdk: flutter

  app_base:
    path: ../../packages/app_base

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true